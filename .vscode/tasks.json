{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Backend: Check & Fix",
      "type": "shell",
      "command": "./scripts/backend_check.bat",
      "problemMatcher": []
    }
  ]
}


// "(yapf -ri ./backend) -AND (isort -rc ./backend) -AND (flake8 ./backend)"
// "yapf -dr ./backend; echo $lastexitcode"
